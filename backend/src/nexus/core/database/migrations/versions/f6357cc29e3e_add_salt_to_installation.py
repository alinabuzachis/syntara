"""add salt to installation

Revision ID: f6357cc29e3e
Revises: 2dbbb22fd04c
Create Date: 2026-04-30 00:00:00.000000

"""

import uuid
from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision: str = "f6357cc29e3e"
down_revision: str | Sequence[str] | None = "2dbbb22fd04c"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Upgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "installation",
        sa.Column("salt", sa.Uuid, nullable=True),
    )
    # ### end Alembic commands ###

    # CUSTOM: Populate the salt for the existing installation row with a random UUID.
    installation_table = sa.table(
        "installation",
        sa.column("salt", sa.Uuid),
    )
    op.execute(installation_table.update().values(salt=uuid.uuid4()))

    # Now enforce NOT NULL.
    op.alter_column("installation", "salt", nullable=False)
    # END CUSTOM


def downgrade() -> None:
    """Downgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("installation", "salt")
    # ### end Alembic commands ###
