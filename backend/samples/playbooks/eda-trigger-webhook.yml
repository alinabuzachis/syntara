---
# Playbook: Trigger an Automation Orchestrator EDA webhook
#
# Called as a rulebook action to fire an AO EDA trigger endpoint.
# Obtains an OAuth token via client credentials, then POSTs the
# event payload to the webhook.
#
# Usage:
#   ansible-playbook eda-trigger-webhook.yml \
#     -e ao_base_url=https://nexus.example.com \
#     -e ao_webhook_path=my-eda-trigger \
#     -e ao_client_id=nx_sa_abc123 \
#     -e ao_client_secret=<secret> \
#     -e alert_name=high_cpu \
#     -e instance=web-server-01 \
#     -e severity=critical

- name: Trigger AO EDA webhook
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    ao_base_url: "https://localhost:8000"
    ao_webhook_path: ""
    ao_client_id: ""
    ao_client_secret: ""
    alert_name: "test_alert"
    instance: "localhost"
    severity: "info"

  tasks:
    - name: Obtain OAuth token from Automation Orchestrator
      ansible.builtin.uri:
        url: "{{ ao_base_url }}/api/v1/auth/token"
        method: POST
        body_format: form-urlencoded
        body:
          grant_type: client_credentials
          client_id: "{{ ao_client_id }}"
          client_secret: "{{ ao_client_secret }}"
        status_code: 200
        validate_certs: "{{ ao_validate_certs | default(true) }}" # set to false for local dev with self-signed certs
      register: token_response

    - name: Post event payload to AO EDA trigger endpoint
      ansible.builtin.uri:
        url: "{{ ao_base_url }}/api/v1/webhooks/eda/{{ ao_webhook_path }}"
        method: POST
        body_format: json
        headers:
          Authorization: "Bearer {{ token_response.json.access_token }}"
          Content-Type: application/json
        body:
          event: "{{ alert_name }}"
          payload:
            instance: "{{ instance }}"
            severity: "{{ severity }}"
            timestamp: "{{ ansible_date_time.iso8601 | default(omit) }}"
        status_code: 202
        validate_certs: "{{ ao_validate_certs | default(true) }}" # set to false for local dev with self-signed certs
      register: webhook_response

    - name: Display execution ID
      ansible.builtin.debug:
        msg: "Workflow execution started: {{ webhook_response.json.execution_id }}"
