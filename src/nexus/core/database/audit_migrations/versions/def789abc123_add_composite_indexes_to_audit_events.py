"""Add composite indexes to audit_events table.

Revision ID: def789abc123
Revises: abc123def456
Create Date: 2026-04-21 12:00:00.000000

"""

from collections.abc import Sequence

from alembic import op

# revision identifiers, used by Alembic.
revision: str = "def789abc123"
down_revision: str | Sequence[str] | None = "abc123def456"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Upgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    # Add composite indexes for common query patterns:
    # filter by field + sort by created_at DESC + cursor pagination using id

    # actor_id queries (e.g., "show me user X's audit events")
    op.create_index(
        "ix_audit_events_actor_id_created_at_id",
        "audit_events",
        ["actor_id", "created_at", "id"],
        unique=False,
    )

    # event_category queries (e.g., "show me workflow_event category")
    op.create_index(
        "ix_audit_events_event_category_created_at_id",
        "audit_events",
        ["event_category", "created_at", "id"],
        unique=False,
    )

    # workflow_id queries (e.g., "show me workflow X's events")
    op.create_index(
        "ix_audit_events_workflow_id_created_at_id",
        "audit_events",
        ["workflow_id", "created_at", "id"],
        unique=False,
    )

    # execution_id queries (e.g., "show me execution X's events")
    op.create_index(
        "ix_audit_events_execution_id_created_at_id",
        "audit_events",
        ["execution_id", "created_at", "id"],
        unique=False,
    )

    # activity_id queries (e.g., "show me activity X's events")
    op.create_index(
        "ix_audit_events_activity_id_created_at_id",
        "audit_events",
        ["activity_id", "created_at", "id"],
        unique=False,
    )

    # event_severity queries (e.g., "show me errors and warnings")
    op.create_index(
        "ix_audit_events_event_severity_created_at_id",
        "audit_events",
        ["event_severity", "created_at", "id"],
        unique=False,
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    """Downgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index("ix_audit_events_event_severity_created_at_id", table_name="audit_events")
    op.drop_index("ix_audit_events_activity_id_created_at_id", table_name="audit_events")
    op.drop_index("ix_audit_events_execution_id_created_at_id", table_name="audit_events")
    op.drop_index("ix_audit_events_workflow_id_created_at_id", table_name="audit_events")
    op.drop_index("ix_audit_events_event_category_created_at_id", table_name="audit_events")
    op.drop_index("ix_audit_events_actor_id_created_at_id", table_name="audit_events")
    # ### end Alembic commands ###
