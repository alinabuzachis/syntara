"""add_refresh_sessions_and_token_version

Revision ID: 6b01a39fb52c
Revises: f6357cc29e3e
Create Date: 2026-04-30 10:21:25.419635

"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision: str = "6b01a39fb52c"
down_revision: str | Sequence[str] | None = "f6357cc29e3e"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Upgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table(
        "refresh_sessions",
        sa.Column("jti", sa.String(length=64), nullable=False),
        sa.Column("user_id", sa.Uuid(), nullable=False),
        sa.Column("issued_at", sa.DateTime(timezone=True), nullable=False),
        sa.Column("expires_at", sa.DateTime(timezone=True), nullable=False),
        sa.Column("revoked_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column("device", sa.String(length=512), nullable=True),
        sa.Column("ip_address", sa.String(length=45), nullable=True),
        sa.Column("amr", sa.JSON(), nullable=True),
        sa.Column("idp", sa.String(length=255), nullable=True),
        sa.Column("idp_id", sa.String(length=36), nullable=True),
        sa.Column("identity_id", sa.String(length=36), nullable=True),
        sa.Column("issuer", sa.String(length=2048), nullable=True),
        sa.Column("subject", sa.String(length=1024), nullable=True),
        sa.Column("id_token_hint", sa.String(length=4096), nullable=True),
        sa.Column("rp_logout_enabled", sa.Boolean(), nullable=False),
        sa.ForeignKeyConstraint(
            ["user_id"],
            ["users.id"],
            ondelete="CASCADE",
        ),
        sa.PrimaryKeyConstraint("jti"),
    )
    op.create_index("ix_refresh_sessions_expires_at", "refresh_sessions", ["expires_at"], unique=False)
    op.create_index(
        "ix_refresh_sessions_identity_id",
        "refresh_sessions",
        ["identity_id"],
        unique=False,
        postgresql_where=sa.text("revoked_at IS NULL"),
    )
    op.create_index(
        "ix_refresh_sessions_idp_id",
        "refresh_sessions",
        ["idp_id"],
        unique=False,
        postgresql_where=sa.text("revoked_at IS NULL"),
    )
    op.create_index(
        "ix_refresh_sessions_user_id",
        "refresh_sessions",
        ["user_id"],
        unique=False,
        postgresql_where=sa.text("revoked_at IS NULL"),
    )
    op.add_column("users", sa.Column("token_version", sa.Integer(), server_default=sa.text("0"), nullable=False))
    # ### end Alembic commands ###


def downgrade() -> None:
    """Downgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("users", "token_version")
    op.drop_index(
        "ix_refresh_sessions_user_id", table_name="refresh_sessions", postgresql_where=sa.text("revoked_at IS NULL")
    )
    op.drop_index(
        "ix_refresh_sessions_idp_id", table_name="refresh_sessions", postgresql_where=sa.text("revoked_at IS NULL")
    )
    op.drop_index(
        "ix_refresh_sessions_identity_id", table_name="refresh_sessions", postgresql_where=sa.text("revoked_at IS NULL")
    )
    op.drop_index("ix_refresh_sessions_expires_at", table_name="refresh_sessions")
    op.drop_table("refresh_sessions")
    # ### end Alembic commands ###
