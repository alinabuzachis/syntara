"""add execution mode and metadata

Revision ID: b889ea197887
Revises: 630011c0284f
Create Date: 2026-05-06 19:42:39.156028

"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

# revision identifiers, used by Alembic.
revision: str = "b889ea197887"
down_revision: str | Sequence[str] | None = "630011c0284f"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Upgrade schema."""
    # CUSTOM: Create enum type before adding column that references it
    executionmode_enum = postgresql.ENUM("standard", "test", "debug", name="executionmode", create_type=True)
    executionmode_enum.create(op.get_bind(), checkfirst=True)
    # END CUSTOM

    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "executions",
        sa.Column(
            "mode",
            postgresql.ENUM("standard", "test", "debug", name="executionmode", create_type=False),
            server_default=sa.text("'standard'::executionmode"),
            nullable=False,
        ),
    )
    op.add_column("executions", sa.Column("execution_metadata", postgresql.JSONB(astext_type=sa.Text()), nullable=True))
    op.create_index(op.f("ix_executions_mode"), "executions", ["mode"], unique=False)
    # ### end Alembic commands ###


def downgrade() -> None:
    """Downgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(op.f("ix_executions_mode"), table_name="executions")
    op.drop_column("executions", "execution_metadata")
    op.drop_column("executions", "mode")
    # ### end Alembic commands ###

    # CUSTOM: Drop enum type after dropping column
    postgresql.ENUM(name="executionmode").drop(op.get_bind(), checkfirst=True)
    # END CUSTOM
