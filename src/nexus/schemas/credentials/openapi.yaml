---
openapi: 3.1.0
info:
  title: Nexus Credentials API
  description: |
    REST API for managing Credentials and Credential Types.

    Credentials are encrypted authentication resources used by workflows,
    chat, and agentic orchestration. Each credential references a Credential
    Type that defines its field schema and injection patterns.

    ## Key Features
    - CRUD operations for Credentials with AES-256-GCM encryption at rest
    - Read-only access to preseeded Credential Types (GA)
    - Secret field masking ($encrypted$) in API responses
    - Partial updates with $encrypted$ sentinel preservation
  version: 1.0.0

servers:
  - url: /api/v1
    description: API v1

tags:
  - name: Credentials
    description: Encrypted credential management
  - name: Credential Types
    description: Credential type schema definitions

paths:
  /credentials:
    get:
      summary: List Credentials
      description: |-
        List Credentials with filtering and pagination. Metadata only, no secrets.
      operationId: list_credentials
      tags:
        - Credentials
      x-app-permission:
        resource: credential
        action: read
        scope: project
        default_roles: [admin, auditor, user, project-admin, project-user, project-auditor]
      parameters:
        - $ref: "../base/shared-resources.openapi.yaml#/components/parameters/limitParam"
        - $ref: "../base/shared-resources.openapi.yaml#/components/parameters/cursorParam"
        - $ref: "../base/shared-resources.openapi.yaml#/components/parameters/sortParam"
        - $ref: "../base/shared-resources.openapi.yaml#/components/parameters/includeTotalParam"
        - name: credential_type_id
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: uuid
              - type: "null"
            description: Filter by credential type ID
            title: Credential Type Id
          description: Filter by credential type ID
        - name: enabled
          in: query
          required: false
          schema:
            anyOf:
              - type: boolean
              - type: "null"
            description: Filter by enabled status
            title: Enabled
          description: Filter by enabled status
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialListResponse"
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
    post:
      summary: Create Credential
      description: |-
        Create a new Credential with encrypted inputs.
      operationId: create_credential
      tags:
        - Credentials
      x-app-permission:
        resource: credential
        action: create
        scope: project
        default_roles: [admin, user, project-admin, project-user]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CredentialCreate"
      responses:
        "201":
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialRead"
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
  /credentials/{credential_id}:
    get:
      summary: Get Credential
      description: |-
        Get a Credential. Secret fields masked as $encrypted$.
      operationId: get_credential
      tags:
        - Credentials
      x-app-permission:
        resource: credential
        action: read
        scope: project
        default_roles: [admin, auditor, user, project-admin, project-user, project-auditor]
      parameters:
        - name: credential_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            title: Credential Id
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialRead"
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
    patch:
      summary: Update Credential
      description: |-
        Update a Credential. Fields set to $encrypted$ retain existing values.
      operationId: update_credential
      tags:
        - Credentials
      x-app-permission:
        resource: credential
        action: update
        scope: project
        default_roles: [admin, user, project-admin, project-user]
      parameters:
        - name: credential_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            title: Credential Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CredentialPatch"
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialRead"
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
    delete:
      summary: Delete Credential
      description: Soft-delete a Credential.
      operationId: delete_credential
      tags:
        - Credentials
      x-app-permission:
        resource: credential
        action: delete
        scope: project
        default_roles: [admin, project-admin]
      parameters:
        - name: credential_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            title: Credential Id
      responses:
        "204":
          description: Successful Response
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
  /credentials/{credential_id}/workflows:
    get:
      summary: Get Credential Workflows
      description: |-
        Get workflows that reference this credential.

        Returns workflows with nodes that have credential_id in their executor configs.
      operationId: get_credential_workflows
      tags:
        - Credentials
      x-app-permission:
        resource: credential
        action: read
        scope: project
        default_roles: [admin, auditor, user, project-admin, project-user, project-auditor]
      parameters:
        - name: credential_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            title: Credential Id
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                title: Response Get Credential Workflows
                items:
                  $ref: "#/components/schemas/CredentialWorkflowRef"
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
  /credential_types:
    get:
      summary: List Credential Types
      description: |-
        List all Credential Types including preseeded managed types.

        Each type includes a credential_count of non-deleted credentials using it.
      operationId: list_credential_types
      tags:
        - Credentials
      x-app-permission:
        resource: null
        action: null
        scope: null
        default_roles: []
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialTypeListResponse"
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
  /credential_types/{credential_type_id}:
    get:
      summary: Get Credential Type
      description: |-
        Get a single Credential Type with credential_count.
      operationId: get_credential_type
      tags:
        - Credentials
      x-app-permission:
        resource: null
        action: null
        scope: null
        default_roles: []
      parameters:
        - name: credential_type_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            title: Credential Type Id
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialTypeRead"
        "400":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/BadRequestError"
        "401":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/UnauthorizedError"
        "403":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ForbiddenError"
        "404":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/NotFoundError"
        "409":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ConflictError"
        "422":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/ValidationError"
        "500":
          $ref: "../base/shared-resources.openapi.yaml#/components/responses/InternalServerError"

      security:
        - bearerAuth: []
components:
  schemas:
    CredentialListResponse:
      allOf:
        - $ref: "../base/shared-resources.openapi.yaml#/components/schemas/ResourcesResponseBase"
        - type: object
          required:
            - resources
          properties:
            resources:
              type: array
              items:
                $ref: '#/components/schemas/CredentialRead'
              title: Resources
              description: Array of resources in current page
      title: CredentialListResponse
      description: Paginated list response for credentials.

    CredentialTypeListResponse:
      allOf:
        - $ref: "../base/shared-resources.openapi.yaml#/components/schemas/ResourcesResponseBase"
        - type: object
          required:
            - resources
          properties:
            resources:
              type: array
              items:
                $ref: '#/components/schemas/CredentialTypeRead'
              title: Resources
              description: Array of resources in current page
      title: CredentialTypeListResponse
      description: Paginated list response for credential types.

    CredentialCreate:
      description: Schema for creating a new credential.
      properties:
        credential_type_id:
          description: ID of the credential type
          format: uuid
          title: Credential Type Id
          type: string
        description:
          anyOf:
            - maxLength: 2000
              type: string
            - type: "null"
          description: Optional description
          title: Description
        inputs:
          additionalProperties: true
          description: Field values validated against type schema
          title: Inputs
          type: object
        labels:
          additionalProperties:
            type: string
          description: Key-value labels
          title: Labels
          type: object
        name:
          description: Human-readable credential name
          maxLength: 255
          minLength: 1
          title: Name
          type: string
        project_id:
          description: Project to assign credential to
          format: uuid
          title: Project Id
          type: string
      required:
        - name
        - credential_type_id
        - project_id
      title: CredentialCreate
      type: object

    CredentialPatch:
      description: Schema for partially updating a credential. $encrypted$ preserves existing values.
      properties:
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
        enabled:
          anyOf:
            - type: boolean
            - type: "null"
          title: Enabled
        inputs:
          anyOf:
            - additionalProperties: true
              type: object
            - type: "null"
          title: Inputs
        labels:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: "null"
          title: Labels
        name:
          anyOf:
            - maxLength: 255
              minLength: 1
              type: string
            - type: "null"
          title: Name
      title: CredentialPatch
      type: object

    CredentialRead:
      description: Schema for credential API responses. Secret fields masked as $encrypted$.
      additionalProperties: false
      allOf:
        - $ref: "../base/shared-resources.openapi.yaml#/components/schemas/Resource"
        - type: object
          title: CredentialRead
          required:
            - credential_type_id
            - project_id
          properties:
            created_by:
              anyOf:
                - type: string
                - type: string
                  format: uuid
                - type: "null"
              description: Username or UUID of the credential creator
              title: Created By
            updated_by:
              anyOf:
                - type: string
                - type: string
                  format: uuid
                - type: "null"
              description: Username or UUID of the last modifier
              title: Updated By
            credential_type_id:
              format: uuid
              title: Credential Type Id
              type: string
            enabled:
              default: true
              title: Enabled
              type: boolean
            inputs:
              additionalProperties: true
              title: Inputs
              type: object
            project_id:
              format: uuid
              title: Project Id
              type: string
            workflow_count:
              default: 0
              description: Number of workflows referencing this credential
              title: Workflow Count
              type: integer

    CredentialTypeRead:
      description: Read schema for credential type API responses.
      additionalProperties: false
      allOf:
        - $ref: "../base/shared-resources.openapi.yaml#/components/schemas/BaseResource"
        - type: object
          title: CredentialTypeRead
          required:
            - name
          properties:
            name:
              title: Name
              type: string
            description:
              anyOf:
                - type: string
                - type: "null"
              title: Description
            credential_count:
              default: 0
              description: Number of non-deleted credentials using this type
              title: Credential Count
              type: integer
            injectors:
              additionalProperties: true
              title: Injectors
              type: object
            inputs:
              additionalProperties: true
              title: Inputs
              type: object
            managed:
              default: false
              title: Managed
              type: boolean

    CredentialWorkflowRef:
      description: Reference to a workflow that uses a credential.
      properties:
        created_by:
          anyOf:
            - type: string
            - format: uuid
              type: string
            - type: "null"
          description: Username or UUID of the workflow creator
          title: Created By
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
        id:
          format: uuid
          title: Id
          type: string
        last_execution_at:
          anyOf:
            - format: date-time
              type: string
            - type: "null"
          description: Timestamp of the most recent execution
          title: Last Execution At
        last_execution_status:
          anyOf:
            - type: string
            - type: "null"
          description: Status of the most recent execution
          title: Last Execution Status
        name:
          title: Name
          type: string
        node_names:
          description: Names of nodes using this credential
          items:
            type: string
          title: Node Names
          type: array
      required:
        - id
        - name
      title: CredentialWorkflowRef
      type: object

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT token authentication

security:
  - bearerAuth: []
