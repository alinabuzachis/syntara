{
  "name": "next-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "concurrently npm:start:* -c auto",
    "start:frontend": "npm run start --prefix packages/frontend",
    "start:genie-ui-framework": "npm run start --prefix packages/genie-ui-framework",
    "build": "npm run build:genie-ui-framework && npm run build:frontend",
    "build:genie-ui-framework": "npm run build --workspace=packages/genie-ui-framework",
    "build:frontend": "npm run build --workspace=packages/frontend",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "docker:build": "docker compose build",
    "docker:run": "docker compose up frontend"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "os": [
    "darwin",
    "linux",
    "windows"
  ],
  "cpu": [
    "x64",
    "arm64"
  ]
}
