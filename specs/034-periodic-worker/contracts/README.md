# Contracts: Shared Periodic Worker

This feature introduces no new REST API endpoints, WebSocket channels, or external-facing contracts.

The PeriodicWorker is an internal infrastructure component consumed programmatically within the codebase. Its interface is defined in `src/nexus/core/workers/periodic.py` and documented in [quickstart.md](../quickstart.md).
