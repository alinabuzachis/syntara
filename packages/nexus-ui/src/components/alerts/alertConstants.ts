/**
 * Shared width constraint for toast alerts and inline alerts that need to match toast sizing.
 * Used in both AlertProvider (toast alerts) and ExecutionDetail (inline failure alert).
 */
export const ALERT_WIDTH = 'clamp(15rem, 20vw, 22rem)'
