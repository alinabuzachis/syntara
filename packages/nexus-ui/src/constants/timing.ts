/**
 * Timing constants for UI interactions.
 */

/**
 * Standard debounce delay for typeahead/search inputs in milliseconds.
 * Used to reduce API calls while user is typing.
 */
export const DEBOUNCE_MS = 300
