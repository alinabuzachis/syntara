/**
 * Default neutral stroke for workflow edges, arrow markers, and automations canvas
 * handles (PatternFly nonstatus gray). Shared so automations code does not import
 * from the builder package.
 */
export const DEFAULT_NEUTRAL_EDGE_STROKE = 'var(--pf-t--global--color--nonstatus--gray--300)'
