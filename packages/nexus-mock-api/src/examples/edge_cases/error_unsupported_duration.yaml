---
schemaVersion: 1.0.0
version: 1

metadata:
  name: error-unsupported-duration
  description: Test error with unsupported duration unit

triggers:
  - type: manual

workflow:
  activities:
    - id: task_with_unsupported_timeout
      name: Task With Unsupported Duration Unit
      type: task
      timeout: PT5D # Days not supported - should raise ValueError
      task:
        executor: script
        config:
          language: bash
          code: echo "Should not execute"
