/**
 * Default neutral stroke for workflow edges, arrow markers, and canvas
 * handles (PatternFly nonstatus gray). Shared so canvas code does not import
 * from the builder package.
 */
export const DEFAULT_NEUTRAL_EDGE_STROKE = 'var(--pf-t--global--color--nonstatus--gray--300)'
