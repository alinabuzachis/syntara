/**
 * Test-only fixtures for password complexity validation.
 * Imported by unit tests only — not used in production bundles.
 */

/** Password that satisfies API-46 rules (14+ chars, four character classes). */
export const COMPLIANT_TEST_PASSWORD = 'ValidPassword123!'
