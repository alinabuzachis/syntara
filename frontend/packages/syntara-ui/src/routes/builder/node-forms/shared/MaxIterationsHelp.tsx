import { nodeHelp } from './nodeFieldHelp'

/**
 * Popover help icon explaining the max iterations parameter for while loops.
 *
 * Used in Loop nodes (while type).
 */
export function MaxIterationsHelp() {
  return nodeHelp.maxIterations
}
